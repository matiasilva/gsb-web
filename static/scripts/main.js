// simple implementation of countdown timer

const targetDate = new Date(2021, 10, 4); // month is 0-indexed

function displayTimer(hello, hello2) {
    
};

const intervalTimer = setInterval(() => {
    const today = new Date();

}, 1000);

